# Official Claude Skills compatible format
name: orchestrator
description: タスクの調整と他のロールへの委譲を行うコーディネーターロール。Workflowエージェントが使用。

# MYCELIUM RBAC extensions
allowedRoles:
  - orchestrator
allowedTools:
  - mycelium-router__list_roles
  # Note: サブエージェントはSDKのTaskツールで生成（spawn_sub_agentは廃止）
  # Note: スキル情報はlist_rolesの結果に含まれる
