# MCP抽出可能性分析：myceliumへの統合検討レポート

> 作成日: 2026-01-24
> 対象: 9つのAIコーディングアシスタント分析結果のmyceliumへの適用

---

## エグゼクティブサマリー

9つのAIコーディングアシスタント（Claude Code、Cursor、Windsurf、Gemini CLI、Codex CLI、clawdbot、GitHub Copilot CLI、Aider、Continue.dev）の機能分析に基づき、myceliumが**既に実現している機能**、**強化すべき機能**、**新規追加すべき機能**を整理した。

### 主要な発見

| カテゴリ | myceliumの現状 | 競合ツールとの比較 |
|---------|--------------|----------------|
| **メモリシステム** | ⭐ 業界最先端 | 唯一のロール分離＋RBAC統合メモリ |
| **スキルシステム** | ⭐ 業界最先端 | 唯一のスキル駆動型RBAC |
| **A2A Identity** | ⭐ 業界最先端 | 唯一のゼロトラストA2A対応 |
| **セッション管理** | ⚠️ 基本的 | 圧縮・フォーク機能が未実装 |
| **スラッシュコマンド** | ⚠️ 限定的 | 拡張可能な仕組みが必要 |
| **サンドボックス** | ❌ 未実装 | Codex CLIが業界標準を設定 |

---

## 1. myceliumの競争優位性（既に実現済み）

### 1.1 スキル駆動型RBAC（唯一の実装）

分析した9ツールの中で、**myceliumのみ**がスキル駆動型RBACを実装している。

```
┌─────────────────────────────────────────────────────────────────┐
│  他ツール: ロール → ツール（管理者が手動定義）                      │
│  mycelium: スキル → ロール → ツール（スキルが宣言、動的生成）        │
└─────────────────────────────────────────────────────────────────┘
```

**競合比較**:

| ツール | 権限モデル | myceliumとの差 |
|-------|---------|--------------|
| Claude Code | なし（全ツール利用可能） | myceliumが圧倒的に優位 |
| Cursor | Project Rules（静的） | myceliumの動的生成が優位 |
| Gemini CLI | なし | myceliumが圧倒的に優位 |
| Codex CLI | 承認モード（ユーザー判断） | myceliumのロールベースが優位 |

**活用提案**: この優位性をマーケティングで強調し、エンタープライズ向け機能として訴求

### 1.2 ロール分離メモリ（唯一の実装）

myceliumのRoleMemoryStoreは、**ロールごとに分離されたメモリ**を提供する唯一のシステム。

| ツール | メモリシステム | ロール分離 | RBAC統合 |
|-------|------------|---------|---------|
| Claude Code | CLAUDE.md（階層的） | ❌ | ❌ |
| Gemini CLI | GEMINI.md（階層的） | ❌ | ❌ |
| Windsurf | 自動生成メモリ | ❌ | ❌ |
| **mycelium** | RoleMemoryStore | ✅ | ✅ |

**現在の実装**:
```yaml
grants:
  memory: isolated  # 自分のロールのみ
  memory: team      # チームメンバーのメモリも閲覧可
  memory: all       # 管理者：全メモリアクセス
```

**活用提案**: 他ツールのメモリ形式（CLAUDE.md、GEMINI.md）との互換レイヤーを追加

### 1.3 A2A Zero-Trust Identity（唯一の実装）

myceliumのIdentityResolverは、**エージェント間通信のゼロトラスト認証**を実現する唯一のシステム。

```yaml
# myceliumのA2A設定例
identity:
  skillMatching:
    - role: admin
      requiredSkills: [admin_access]      # AND条件
      forbiddenSkills: [trial_user]       # 禁止条件
      context:
        allowedTime: "09:00-18:00"        # 時間制限
        timezone: "Asia/Tokyo"
```

**競合との差**: 他のツールはエージェント間通信のセキュリティを考慮していない

---

## 2. 強化すべき機能（ギャップ分析）

### 2.1 セッション管理の高度化

**現状**: 基本的なREPLセッション維持のみ

**競合の実装**:

| ツール | セッション機能 |
|-------|------------|
| Claude Code | `/compact`（コンテキスト圧縮）、自動保存 |
| Gemini CLI | `/chat save\|resume\|list\|delete\|share`、`/compress` |
| Codex CLI | `--resume`、`/undo` |
| Aider | `/save`、`/load`、履歴管理 |

**提案: Session MCP Server の実装**

```typescript
// 新規ツール提案
interface SessionTools {
  // セッション管理
  'session_save': { name?: string, tags?: string[] };
  'session_resume': { session_id: string };
  'session_list': { filter?: { tags?: string[], date_range?: DateRange } };
  'session_delete': { session_id: string };
  'session_fork': { session_id: string, from_message?: number };

  // コンテキスト圧縮
  'session_compress': {
    strategy: 'summarize' | 'truncate' | 'semantic';
    target_tokens?: number;
  };

  // エクスポート
  'session_export': {
    format: 'markdown' | 'json' | 'html';
    include_tool_calls?: boolean;
  };
}
```

**優先度**: 🔴 高（ユーザー体験に直結）

### 2.2 スラッシュコマンドの拡張性

**現状**: `/roles`、`/tools`、`/model`、`/status`、`/help`、`/quit`、`/skills`

**競合との比較**:

| ツール | コマンド数 | カスタムコマンド |
|-------|---------|--------------|
| Claude Code | 21+ | ✅ `.claude/commands/` |
| Gemini CLI | 30+ | ❌ |
| Codex CLI | 16 | ✅ `~/.codex/prompts/` |
| **mycelium** | 7 | ⚠️ スキル経由のみ |

**提案: Skills/Commands MCP Server の強化**

現在の`@aegis/skills`を拡張し、スラッシュコマンドを動的に生成:

```yaml
# skills/custom-command/SKILL.md
---
id: git-workflow
displayName: Git Workflow Commands
commands:
  - name: commit
    description: AI生成コミットメッセージでコミット
    script: scripts/smart-commit.sh
  - name: pr
    description: PRを作成してレビュー依頼
    script: scripts/create-pr.sh
allowedRoles: [developer]
---
```

**CLI側の実装**:
```typescript
// InteractiveCLI拡張
async loadSkillCommands(): Promise<Map<string, SkillCommand>> {
  const skills = await this.router.listSkills();
  const commands = new Map();

  for (const skill of skills) {
    if (skill.commands) {
      for (const cmd of skill.commands) {
        commands.set(`/${cmd.name}`, {
          ...cmd,
          skillId: skill.id,
        });
      }
    }
  }
  return commands;
}
```

**優先度**: 🟡 中（ユーザビリティ向上）

### 2.3 Thinking Signatureの活用拡大

**現状**: AuditLoggerでThinkingSignatureを記録

**提案**: 他ツールにない独自機能として強化

```typescript
// 新規ツール提案: Thinking Analysis
interface ThinkingAnalysisTools {
  // 思考パターン分析
  'thinking_analyze': {
    time_range?: DateRange;
    role?: string;
    tool?: string;
  };

  // 異常検知
  'thinking_detect_anomaly': {
    baseline_sessions?: string[];
    threshold?: number;
  };

  // コンプライアンスレポート
  'thinking_compliance_report': {
    format: 'pdf' | 'html' | 'json';
    include_full_thinking?: boolean;
  };
}
```

**優先度**: 🟡 中（エンタープライズ差別化）

---

## 3. 新規追加すべき機能

### 3.1 サンドボックス実行環境

**競合の実装（Codex CLI）**:

| プラットフォーム | 技術 | 詳細 |
|-------------|-----|-----|
| macOS | Seatbelt | Apple `sandbox-exec` |
| Linux | Landlock + seccomp | 2層防御 |
| Windows | WSL2 | Linuxサンドボックス経由 |

**myceliumへの統合提案**:

```typescript
// packages/sandbox/src/index.ts
export interface SandboxConfig {
  platform: 'macos' | 'linux' | 'windows';

  // ファイルシステム制限
  allowedPaths: {
    read: string[];
    write: string[];
  };

  // ネットワーク制限
  network: {
    allowOutbound: boolean;
    allowedHosts?: string[];
  };

  // プロセス制限
  process: {
    maxMemoryMB: number;
    timeoutSeconds: number;
    allowFork: boolean;
  };
}

// スキルでのサンドボックス指定
interface SkillDefinition {
  // ... 既存フィールド
  sandbox?: {
    enabled: boolean;
    profile: 'strict' | 'standard' | 'permissive';
    customConfig?: Partial<SandboxConfig>;
  };
}
```

**ロールベース統合**:
```yaml
# skills/untrusted-code-runner/SKILL.md
---
id: untrusted-code-runner
allowedRoles: [sandbox-runner]
sandbox:
  enabled: true
  profile: strict
  customConfig:
    allowedPaths:
      read: ["/tmp/sandbox"]
      write: ["/tmp/sandbox/output"]
    network:
      allowOutbound: false
---
```

**優先度**: 🔴 高（セキュリティ要件）

### 3.2 他ツールのメモリ形式との互換レイヤー

**提案: Memory Format Adapter**

```typescript
// packages/memory-compat/src/adapters/
export interface MemoryFormatAdapter {
  // 読み込み
  parse(content: string): MemoryEntry[];

  // 書き出し
  serialize(entries: MemoryEntry[]): string;

  // フォーマット検出
  detect(filePath: string): boolean;
}

// 実装するアダプター
export const adapters = {
  'claude-md': ClaudeMdAdapter,      // CLAUDE.md形式
  'gemini-md': GeminiMdAdapter,      // GEMINI.md形式
  'agents-md': AgentsMdAdapter,      // AGENTS.md形式（Codex CLI）
  'cursor-rules': CursorRulesAdapter, // .cursorrules形式
  'mycelium': MyceliumAdapter,        // ネイティブ形式
};
```

**マイグレーションツール**:
```bash
# 他ツールからの移行
mycelium migrate --from claude-md --file CLAUDE.md
mycelium migrate --from gemini-md --file GEMINI.md

# 他ツール向けエクスポート
mycelium export --to claude-md --role developer
```

**優先度**: 🟡 中（エコシステム統合）

### 3.3 コードベース検索の強化

**競合の実装**:

| ツール | 検索機能 |
|-------|--------|
| Cursor | セマンティック検索（埋め込み） |
| Windsurf | Codebase Investigator |
| Continue.dev | @Codebase（非推奨→MCP移行中） |
| Aider | Repository Map（tree-sitter） |

**提案: Codebase Search MCP Server**

```typescript
// @mycelium/codebase-search
interface CodebaseSearchTools {
  // セマンティック検索
  'codebase_semantic_search': {
    query: string;
    file_patterns?: string[];
    max_results?: number;
  };

  // シンボル検索
  'codebase_symbol_search': {
    symbol: string;
    kind?: 'function' | 'class' | 'variable' | 'type';
  };

  // リポジトリマップ生成
  'codebase_generate_map': {
    format: 'tree' | 'graph' | 'summary';
    include_dependencies?: boolean;
  };

  // インデックス管理
  'codebase_index_status': {};
  'codebase_reindex': { paths?: string[] };
}
```

**優先度**: 🟡 中（開発者体験向上）

---

## 4. 統合ロードマップ

### Phase 1: セッション管理強化（2-3週間）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 1-2: Session MCP Server 実装                          │
│  - session_save / session_resume / session_list             │
│  - session_compress（summarize戦略）                         │
│  - CLIへの /save, /resume, /compress コマンド追加            │
├─────────────────────────────────────────────────────────────┤
│  Week 3: テスト・ドキュメント                                 │
│  - 既存セッションからのマイグレーション                         │
│  - Red Team検証（セッションデータ漏洩テスト）                   │
└─────────────────────────────────────────────────────────────┘
```

### Phase 2: スラッシュコマンド拡張（1-2週間）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 1: Skills → Commands 変換                              │
│  - SKILL.md に commands フィールド追加                        │
│  - InteractiveCLI でのコマンド動的ロード                       │
├─────────────────────────────────────────────────────────────┤
│  Week 2: 組み込みコマンド追加                                  │
│  - /compact, /diff, /undo, /commit                          │
│  - RBAC統合（ロールによるコマンド制限）                         │
└─────────────────────────────────────────────────────────────┘
```

### Phase 3: サンドボックス実装（3-4週間）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 1-2: @mycelium/sandbox パッケージ作成                   │
│  - Linux: Landlock + seccomp                                │
│  - macOS: Seatbelt                                          │
├─────────────────────────────────────────────────────────────┤
│  Week 3: スキル・ロール統合                                    │
│  - sandbox フィールドをスキル定義に追加                         │
│  - ロールごとのサンドボックスプロファイル                        │
├─────────────────────────────────────────────────────────────┤
│  Week 4: Red Team検証                                        │
│  - サンドボックスエスケープテスト                               │
│  - 権限昇格テスト                                             │
└─────────────────────────────────────────────────────────────┘
```

### Phase 4: エコシステム統合（2-3週間）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 1: Memory Format Adapters                              │
│  - CLAUDE.md / GEMINI.md / AGENTS.md パーサー                │
│  - マイグレーションCLIコマンド                                 │
├─────────────────────────────────────────────────────────────┤
│  Week 2-3: Codebase Search MCP Server                        │
│  - セマンティック検索（埋め込みモデル選択可能）                   │
│  - Repository Map生成                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 実装優先度マトリックス

| 機能 | 影響度 | 実装難易度 | 優先度 | 理由 |
|-----|-------|---------|-------|-----|
| **Session MCP Server** | 高 | 中 | 🔴 P0 | ユーザー体験に直結 |
| **サンドボックス** | 高 | 高 | 🔴 P0 | セキュリティ要件 |
| **スラッシュコマンド拡張** | 中 | 低 | 🟡 P1 | 既存アーキテクチャで対応可能 |
| **Memory互換レイヤー** | 中 | 中 | 🟡 P1 | 他ツールからの移行促進 |
| **Codebase Search** | 中 | 高 | 🟢 P2 | 既存MCPサーバーで代替可能 |
| **Thinking分析ツール** | 低 | 中 | 🟢 P2 | エンタープライズ向け差別化 |

---

## 6. 結論

myceliumは**スキル駆動型RBAC**、**ロール分離メモリ**、**A2A Zero-Trust Identity**という3つの独自機能で業界をリードしている。

これらの優位性を維持しつつ、以下の機能を追加することで、競合ツールの利点を取り込みながら差別化を強化できる：

1. **セッション管理の高度化** - Claude Code/Gemini CLIの`/compact`相当機能
2. **サンドボックス実行** - Codex CLIのLandlock/Seatbelt相当機能
3. **スラッシュコマンド拡張** - スキルベースのカスタムコマンド
4. **メモリ形式互換レイヤー** - 他ツールからの移行支援

これにより、myceliumは「薄いUIクライアント」を実現するMCPエコシステムの中核として、**最もセキュアで拡張性の高いMCPルーター**としての地位を確立できる。
